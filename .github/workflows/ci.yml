name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docker:
    name: docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build production image
        run: docker build --tag boniluan-home:test .
